# Apple-Picker

In order to start it correctly, I had to go to File > add both Start Scene (with index 0) and Sample Scene (with index 1). To do this, open each scene individually, then click the "Add Open Scenes" button twice, one on each scene. Then, go back and open the "Start Scene" and press the play button, everything should work then